apiVersion: apps/v1
kind: Deployment
metadata:
  name: kanbunny
  namespace: kanbunny
  labels:
    app: kanbunny
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kanbunny
  template:
    metadata:
      labels:
        app: kanbunny
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - optiplex2
      containers:
      - name: kanbunny
        image: localhost/kanbunny:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3500
          name: http
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3500"
        - name: HOST
          value: "0.0.0.0"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: kanbunny-postgres
              key: DATABASE_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        readinessProbe:
          httpGet:
            path: /healthz
            port: 3500
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3500
          initialDelaySeconds: 15
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: kanbunny
  namespace: kanbunny
spec:
  selector:
    app: kanbunny
  ports:
  - port: 3500
    targetPort: 3500
    nodePort: 30083
    name: http
  type: NodePort
