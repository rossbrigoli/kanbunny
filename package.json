{
  "name": "kanbunny",
  "version": "1.0.0",
  "description": "Simple Kanban board for project tracking",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate:sqlite-to-postgres": "node ops/migrate-sqlite-to-postgres.js",
    "pretest": "bash tests/setup-test-pg.sh",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openid-client": "^5.7.1",
    "pg": "^8.23.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
